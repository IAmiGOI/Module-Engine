# ST Module Engine — Beta: каталог Библиотек

Черновой каталог, по мотивам [ARCHITECTURE.md](ARCHITECTURE.md) и
[CORES.md](CORES.md). Библиотека — чистый хелпер, без состояния, без
внешнего мира (в т.ч. без ST, DOM, сети — это уже за границей Библиотеки, в
Сервисе). Задача — типовые функции, упрощающие/стандартизирующие написание
модулей и ядер, не более. Три лагеря: **модулей**, **ядер**, **общие**
(используются и теми, и другими).

## Принцип разработки: Библиотека — ПЕРЕД кодом Ядра/Модуля

**Приоритет — сперва выделить/написать Библиотеку, и только потом писать
код внутри конкретного Ядра/Модуля, который её использует**, а не наоборот
(не писать логику инлайном "как получится", вынося в библиотеку задним
числом при рефакторинге). Если что-то похоже на переиспользуемый паттерн —
сначала Библиотека, потом код, который её потребляет. Это прямое продолжение
приоритетов №1 (простота) и №2 (кастомизация) — Ядро/Модуль пишется тривиально,
если нужная Библиотека уже существует.

## Уже подтверждено по ходу обсуждения

| Библиотека | Лагерь | Статус | Назначение |
|---|---|---|---|
| **Офлайн-фейки шин** | Общая | ✅ [libraries/shared/fake-buses.js](libraries/shared/fake-buses.js) | Поддельные реализации 5 Шин (события/модули/ядра/сервисы/сеть) для тестирования без реального движка — см. ROADMAP.md шаг 1. Финального UI фейка ещё нет. |
| **Готовые виджеты** (Toggle/Select/DraggableList/Chip/SliderField...) | Общая | Не начато | Поверх примитивов Ядра UI (реактивность/диффинг — уже готовы, см. CORES.md). |
| **`request()` поверх `subscribe()`** | Общая | ✅ [libraries/shared/request.js](libraries/shared/request.js) | Разовое ожидание + опциональный таймаут — обёртка, не примитив Шины. Первый реальный потребитель — Ядро финального UI для PC (каждый вызов `dom.*` идёт через это). |
| **Гейт для сети** | Общая | ✅ [libraries/shared/network-gate.js](libraries/shared/network-gate.js) | Композиция обычного Гейта + отдельная проверка `hasNetworkAccess()` (бинарное право, не часть уровня доверия) — см. ARCHITECTURE.md "Сетевой доступ". Работает только поверх физически отдельной Шины сети (`buses.network` в engine.js), иначе проверку можно обойти через обычный Гейт сервисов. |
| **Неймспейсинг поверх сырого объекта** | Ядра | ✅ [libraries/core/namespaced-store.js](libraries/core/namespaced-store.js) | "Кто что пишет" (ARCHITECTURE.md "Персистентность") — чистые функции над переданным объектом, ничего не знают о ЧЁМ хранится. Два реальных потребителя: [cores/memory/index.js](cores/memory/index.js) (chatMetadata) и [cores/settings/index.js](cores/settings/index.js) (extensionSettings) — ровно тот повторный сценарий, ради которого библиотека и была выделена заранее. |
| **Форма снапшота бэкапа** | Ядра | ✅ [libraries/core/backup-snapshot.js](libraries/core/backup-snapshot.js) | Собирает несколько именованных источников в один версионированный объект (`build`) и защищённо читает его обратно (`resolve`/`isValid`) — ничего не знает об источниках, файлах или Сервисах. Потребитель — [cores/backup/index.js](cores/backup/index.js). |
| **Подключение к моделям** (очередь + балансировка по нагрузке + диспатч) | Ядра | ✅ [libraries/core/dispatch-queue.js](libraries/core/dispatch-queue.js) | Общий паттерн для всех 5 модельных Ядер (внутренние/локал-хост/эмбеддинг/TTS/diffusion) — не знает ничего о HTTP или форме запроса, только "какой воркер и когда". Первый реальный потребитель — [cores/models/internal-engine.js](cores/models/internal-engine.js). |
| **Формат запроса/ответа HTTP-провайдеров** (openai/anthropic/google) | Ядра | ✅ [libraries/core/provider-request.js](libraries/core/provider-request.js) | Чистое построение `{url,method,headers,body}` + разбор ответа, без сети — реальный HTTP-вызов делает Сервис HTTP через `host.network`. Появилась по факту первого реального модельного Ядра, не была предугадана заранее в этой таблице. **Ризонинг — три РАЗНЫХ настоящих API**: OpenRouter'овский unified `reasoning` (только на реальный OpenRouter-эндпоинт — просто "OpenAI-совместимый" не получает ничего, единого стандарта нет), настоящий Anthropic `thinking` (бюджет ≥ 1024 и строго меньше `max_tokens` — оба ограничения проверены здесь же, а не оставлены на откуп ошибке от самого Anthropic), настоящий Google `thinkingConfig`. `inherit` не шлёт ни байта ни для одного формата. |
| **Реестр UI-монтирования** (ключ → свой независимый Final UI, mount/unmount/unmountAll) | Общая | ✅ [libraries/shared/ui-mount-registry.js](libraries/shared/ui-mount-registry.js) | *(Не была предугадана заранее — найдена по ходу.)* Общий механизм под Ядром UI для Engine (именованные слоты) и Ядром UI модулей (неограниченный реестр) — каждый ключ обязан получить СВОЙ Final UI, иначе пути деревьев сталкиваются в одной карте. |
| **Шаблонизация текста** (`fillTemplate`-стиль: `{field}` → строка) | Ядра | ✅ [libraries/core/fill-template.js](libraries/core/fill-template.js) | Была предугадана в этом файле заранее (см. историю ниже) — первый реальный потребитель: [cores/tracking/index.js](cores/tracking/index.js)'s сборка промпта опроса. |
| **Разбор ответа модели** (снять markdown-код-забор, вытащить JSON) | Ядра | ✅ [libraries/core/parse-model-json.js](libraries/core/parse-model-json.js) | Тоже была предугадана заранее — Alpha's `parseTrackerResponse`, обобщённый. Первый потребитель — то же Ядро трекинга; годится любому Ядру, ожидающему структурированный JSON-ответ от модели. |
| **Упаковка записей в чанки по токенному бюджету** (целиком, по порядку, без усечения) | Ядра | ✅ [libraries/core/entry-chunker.js](libraries/core/entry-chunker.js) | Первый потребитель — чанкованный бутстрап Графа памяти (Проходы 1-2); годится любому многовызовному проходу над большим списком записей. |
| **Мини-язык для макросов** (токенизатор/парсер/интерпретатор) | Ядра | ✅ [libraries/core/macro-language.js](libraries/core/macro-language.js) | Перенесена из Alpha's modules/macros/language.js практически без изменений — та Библиотека уже была чистой (без ST/DOM/сети). Потребитель — [cores/macros/index.js](cores/macros/index.js). |
| **Фильтры доставки** (every N / debounce / throttle / dedupe) | Общая | ✅ [libraries/shared/delivery-filters.js](libraries/shared/delivery-filters.js) | Та обвязка, которую в Alpha каждый модуль писал у себя счётчиками и таймерами. Применяет её Директор ([contract-bus.js](libraries/shared/contract-bus.js)) как часть словаря `when`, поэтому в Ядрах/Модулях её нет вообще: тайминг и счёт — не их работа (приоритеты №1 и №3). |
| **Персистентный список конфигурации** (`configureX()`-сеттер ↔ `storage.settings`) | Ядра | ✅ [libraries/core/persisted-list.js](libraries/core/persisted-list.js) | Закрывает "config сбрасывается при перезагрузке" — стаб, который был у каждого `configureX()`-Ядра до появления Ядра сохранения. `restore()` читает и применяет сохранённое ОДИН раз, явно, при сборке движка (не из конструктора Ядра — гонялось бы с вызовом `configureX()` самим вызывающим кодом); `save()` применяет в памяти И пишет через. Три реальных потребителя: [cores/models/internal-engine.js](cores/models/internal-engine.js) (`workers`), [cores/tracking/index.js](cores/tracking/index.js) (`trackers`), [cores/macros/index.js](cores/macros/index.js) (`programs`) — проверено не только тестами, но настоящей перезагрузкой страницы браузерного харнесса (localStorage-backed фейковый ST-контекст). |
| **Виджеты UI** — правила их применения в [UI.md](UI.md) — (Button/TextInput/TextArea/NumberInput/**Slider**/Select/**Toggle**/**ColorPicker**/Chip/Details/Field/Row/**Card+Section**/Badge/**Toast+FloatingStack**/**Overlay+Spinner+Banner**/EmptyState/List/TwoColumn/EditableList) | Общая | ✅ [libraries/shared/widgets.js](libraries/shared/widgets.js) | *(Не была предугадана заранее.)* Чистые функции над `h()` — ни DOM, ни ST, ни шин внутри, поэтому годятся любому UI-потребителю, а не только панели движка. Появилась по прямому требованию: «Если пишешь UI — пиши лучше сразу в библиотеки, чтобы это было общим», то есть ни один экран не имеет права держать свою собственную кнопку/поле. Первые потребители — [экран движка](cores/ui/engine-panel.js) и [Модуль «Трекер»](modules/tracker/index.js). **Card и Section — намеренно разные вещи**: рамку, фон и тень рисует ТОЛЬКО верхний уровень (Card), всё внутри — плоский Section с волосяной линией и акцентной засечкой. Найдено по факту: «Модули → Трекер → Трекер → status» дало четыре вложенных обрамления, в которых не видно, что к чему относится. **Стиль подтянут из Alpha** (её style.css): переменная-акцент, шкала радиусов, `Slider` с живым значением, `Toggle` как настоящий тумблер (дорожка + бегунок, состояние берётся из скрытого чекбокса, а не дублируется классом), чипы с акцентной рамкой — и починка `.menu_button`, которому ST задаёт `width: min-content`, отчего многословная подпись схлопывалась в узкий столбик. **Иерархию держит ФОН, а не рамки**: шкала глубины `--stme-level-1..3`, где вложенность берётся из реального DOM (`.stme-section .stme-section` сама на ступень глубже), поля ввода при этом СВЕТЛЕЕ подложки, иначе на нижнем уровне тёмное поле на тёмном фоне перестаёт читаться. Цвет даёт акцентный градиент в шапке карточки — приём Alpha; до него шапка была градиентом из прозрачного в чёрный, то есть цвета не было вовсе. **Toast, Banner и Overlay — три РАЗНЫХ обещания, а не три вида плашки**: плашка временная и уезжает сама; полоса висит, пока причина не устранена, и несёт действие, которым её устраняют; перекрытие отнимает доступ к странице целиком и закрывать его пользователю нечем — так и задумано. `Select()` помечает выбранный вариант на самом `<option>`, а не через `value` на `<select>`: пропсы применяются раньше, чем существуют дети, так что `value` было бы записано в пустой список. **`Select()` умеет `onChange`** (та же пара, что уже есть у `Toggle()`) — понадобилось пресету сэмплера: выбор обязан не только запомниться, но и заполнить соседние поля, а не только свой сигнал. **`Field()`'s `hint` может быть сигналом**, не только голой строкой, — подсказка пресета обязана следить за тем, какой пресет выбран СЕЙЧАС, а сама карточка воркера при этом не перерисовывается заново (`EditableList` зовёт `renderItem` только когда меняется САМ массив, не поле внутри одной записи); строковая ветка осталась нетронутой — иначе `computed()`, даже пустой, остаётся ФУНКЦИЕЙ и не даёт `h()`'ову фильтру `null`/`undefined`/`false` выбросить пустой узел, как выбрасывал раньше. **`ColorPicker()`** (ROADMAP.md 5.39) — нативный `<input type=color>` + текстовое hex-поле рядом, оба на одном сигнале: пикер удобен мышью, поле — когда цвет нужно ввести/сверить точно (например из применённого пресета говорящих). Первый потребитель — [Модуль «Speaker Colors»](modules/speaker-colors/index.js). **`FloatingPanel()` — `className`/`minWidth`/`minHeight`** (ROADMAP.md 5.43, аддитивные пропы): `className` даёт per-instance CSS-вариант (`stme-floating-panel--map` — окно, открывающееся почти во весь вьюпорт) без дублирования виджета; `minWidth`/`minHeight` пишутся в инлайн-стиль ВСЕГДА (в отличие от `width`/`height`, которые молчат до первого реального resize) — жёсткий пол размера для окна, которое нельзя сжать меньше рабочего минимума. **`DockButton()`/`EdgeDrawer()`** (тоже 5.43) — два новых виджета: круглая кнопка, фиксированная на ЭКРАНЕ (не в панели), с тем же приёмом позиционирования, что у `FloatingPanel` (CSS-дефолт правый нижний угол, инлайн `left/top` только после перетаскивания) — первый потребитель, у которого сама "ручка" перетаскивания И ЕСТЬ кликабельная кнопка (см. правку `draggable.js` ниже); и тонкий выступ-вкладка сбоку контейнера, разворачивающая панель настроек по КЛИКУ (не по наведению — это дело общего `.stme-launcher-dock` из `index.js`, отдельная сущность). Первый потребитель обоих — [Модуль «Карта»](modules/map/index.js). **`IconButton()`** (ROADMAP.md 5.47) — круглая кнопка ТОЛЬКО с иконкой (никогда `menu_button`-хром), с `active`-состоянием для палитры инструментов; иконка — настоящий inline SVG (пин с отверстием/пятиугольник для маркера/региона у Карты), не эмодзи. |
| **Исполнение объявленного графа этапов** (уровни зависимостей + цепочка попыток + fold/collect) | Ядра | ✅ [libraries/core/pipeline-runner.js](libraries/core/pipeline-runner.js) | Чистая: ни шин, ни Гейтов, ни ST, ни своих таймеров сверх инъектируемого `sleep` — саму работу отдаёт наружу через `execute`. Потребитель — [Ядро пайплайнов](cores/pipeline/index.js), но паттерн шире генерации: любому Ядру/Модулю, которому нужны «этапы с зависимостями и внятным поведением при отказе», не придётся писать это у себя (приоритеты №1 и №3, как с `when` у Директора). `computeStageLevels()` кладёт независимое в один уровень, `buildAttempts()` разворачивает `fallbacks` в цепочку попыток с наследованием контракта и параметров, `resolveStageParams()` подставляет выходы предыдущих этапов по `{ $from }`. |
| **Память о свёрнутом/раскрытом** | Общая | ✅ [libraries/shared/collapse-state.js](libraries/shared/collapse-state.js) | Живёт отдельно от виджетов намеренно: `Card`/`Section` — ЧИСТЫЕ функции и не вправе ни ходить на шину, ни что-то сохранять, поэтому им отдаётся готовая пара «сигнал + что делать при переключении», а откуда она взялась — не их дело. По умолчанию свёрнуто всё (экран движка целиком на страницу не помещается). Ключ у каждой сворачиваемой штуки стабильный, иначе состояние переезжало бы на соседа при добавлении элемента в список. Два потребителя со СВОИМИ неймспейсами: [панель движка](cores/ui/engine-panel.js) и [Модуль «Трекер»](modules/tracker/index.js) — как и шаблоны отображения, это настройка того, кто рисует, а не конфигурация Ядра.
| **Перетаскивание плавающих окон** | Общая | ✅ [libraries/shared/draggable.js](libraries/shared/draggable.js) | Поведение, а не виджет: набор обработчиков на «ручку» плюс `clampToViewport()`. У Alpha перетаскивались HUD трекера, dev-panel и плеер, и каждый писал это у себя. Читает координаты У СОБЫТИЯ (как любое поле читает `event.target.value`), узлов не ищет. Три неочевидности, каждая стоила бы бага: захват указателя (иначе окно теряется при быстром движении), начальная точка с экрана при первом перетаскивании (иначе телепорт в угол), подрезка по экрану при отпускании (иначе окно уносит за край собственную шапку). **`onClick` (ROADMAP.md 5.43)** — аддитивный параметр: отличает "почти неподвижное отпускание" (клик) от "утащили" (drag) по порогу `clickThresholdPx` (дефолт 4px), `onDrop` по-прежнему срабатывает на каждом отпускании как раньше. **Реальный баг, пойманный вживую в браузерном харнессе** (не тестами — юнит-тесты этого места молчали, потому что писались с той же неверной моделью): защита "клик по кнопке ВНУТРИ ручки не запускает drag" (`event.target.closest('button')`) считала ручку-которая-САМА-кнопка (`DockButton`) кнопкой внутри самой себя и глушила вообще любой клик/drag по ней. Исправлено проверкой `event.target !== event.currentTarget` перед `closest()`. Первый потребитель `onClick` — кнопка дока [Модуля «Карта»](modules/map/index.js).
| **Карточка «Сэмплер + ризонинг + пресет»** | Общая | ✅ [libraries/shared/generation-settings-panel.js](libraries/shared/generation-settings-panel.js) | Появилась по факту РАСХОЖДЕНИЯ, не была предугадана: Модуль «Трекер» и Модуль «RP Time» рисовали одну и ту же по смыслу карточку каждый у себя, и после одной самостоятельной правки они уже разошлись — у одного сворачивалась в `Details()`, у другого стояла прямо в общем блоке. `GenerationSettingsPanel(record, presets, {onApplyPreset, onSavePreset, onDeletePreset})` теперь ОДНА. Как и `Card`/`Section` (см. `collapse-state.js`), она ЧИСТАЯ — не ходит на шину сама, только собирает вёрстку из готовых виджетов и зовёт переданные колбэки; список пресетов приходит параметром (тем же приёмом, что `options` у `Select()`), а не импортом из Ядра моделей — иначе Библиотека оказалась бы НИЖЕ Ядра, которое её использует. Оба Модуля-потребителя ([tracker](modules/tracker/index.js), [time](modules/time/index.js)) сами собирают `[...SAMPLER_PRESETS, ...customPresets()]` и передают его сюда.
| **Выбор момента → условие `when` Директора** (`TRIGGER_MODES`/`computeTriggers`/`resolveTriggerMode`) | Ядра | ✅ [libraries/core/trigger-modes.js](libraries/core/trigger-modes.js) | Вынесена из `modules/tracker/index.js`, когда тот же выбор понадобился Ядру macros — второй реальный потребитель, ровно тот сценарий, ради которого Библиотеки и выделяются заранее (см. принцип в начале файла). Generic-форма: manual/beforeSend/onUserMessage/onToolCall/everyReply/everyNReplies/everyNMinutes → `{event}`/`{event,every}`/`{every:{ms}}`. Здесь НЕТ понятия "blocking"/этапа пайплайна — только у Трекера `beforeSend` умеет стать ЭТАПОМ `generation.prepare` (ждать перед ответом), потому что только он вкладывается в промпт; `modules/tracker/index.js`'s собственные `computeTriggers()`/`resolveTriggerMode()` теперь тонкая обёртка поверх этой Библиотеки, добавляющая ровно этот случай. Ядро macros зовёт её напрямую, без обёртки — макрос никуда не вкладывается, блокировать генерацию ради него нечем и незачем.
| **Типовая обвязка Модуля** (`createModuleHost`/`persistedSettings`/`chatCollection`/`pipelineStage`/`stTool`) | Общая | ✅ [libraries/shared/module-kit.js](libraries/shared/module-kit.js) | То, что каждый из пяти Модулей писал у себя руками: разовый вызов+notify, персистентные настройки с клампером, память чата, этап пайплайна, инструмент ST. Набор НЕЗАВИСИМЫХ хелперов (решение пользователя), а не единая фабрика — Модуль берёт только нужное. `chatCollection` встроенно закрывает ОБА класса персистентных багов: flush после каждой записи (класс 1 — NoteBook до переноса flush'а НЕ делал, живой баг) и подписка `st.chatChanged` (класс 2). `pipelineStage` регистрирует контракт на `host.own` + этап одной парой; `stTool` умеет `onError: 'returnText'` (ошибка модели возвращается текстом, генерацию не обрывает). Первый потребитель — [под-модуль NoteBook Модуля «Tools»](modules/tools/notebook.js); тесты — [tests/module-kit.test.js](tests/module-kit.test.js). |
| **Разрешение источников World Info** (`resolveBookNames`/`summarizeEntry`/`matchesFilter`/`mergeBooks`/`nextUid`/`blankEntry`) | Ядра | ✅ [libraries/core/lorebook-sources.js](libraries/core/lorebook-sources.js) | Портирована из Alpha's `core/lorebook-service.js` (там была смешана с прямыми ST-вызовами в одном файле — по правилам этого движка обязана была разъехаться). `resolveBookNames(snapshot)` — чистая, над ПЛОСКИМ снимком сырых полей (не над живым `context`, как у Alpha): снимок собирает [services/st-lorebook.js](services/st-lorebook.js), сюда попадают уже голые данные. `blankEntry()` зеркалит настоящий `newWorldInfoEntryDefinition` ST (35+ полей — `depth: 4`/`groupWeight: 100`/`selectiveLogic: 0` и другие, все значения сверены с актуальным исходником `world-info.js`, не выдуманы), не 11 хардкоженных, что были у Alpha. Единственный реальный потребитель — [Ядро работы с WI](cores/lorebook/index.js).
| **Сопоставление реплик с заданным составом персонажей** (`detectSpeakers`/`findKnownMention`/`splitIntoSentences`/`findSpeechVerbSubject`/`findSentenceSubject`/`findVocativeAddressee`/`addCastMember`/`removeCastMember`/`updateCastMember`/`hasPresenceVeto`/`findDepartureSubject`) | Ядра | ✅ [libraries/core/speaker-detection.js](libraries/core/speaker-detection.js) | Написана ДО [Ядра определения говорящего](cores/speaker/index.js), как требует принцип этого файла. Локально, без LLM — атрибуция реплики ПО ПРЕДЛОЖЕНИЮ (turn-taking по чередованию сознательно НЕ применяется — ломается уже на третьем говорящем в одном абзаце). **Closed-set, не open-set** (ROADMAP.md 5.40, поправка к первой версии 5.39): состав персонажей (имя+**пол**, задаёт пользователь) — фиксированный вход, `detectSpeakers()` его НИКОГДА не меняет и не создаёт новых персонажей из прозы — единственный примитив поиска имени, `findKnownMention()`, ищет буквальное вхождение уже известного алиаса и физически не может вернуть id, которого нет в составе. Первая версия пыталась угадывать "похоже ли слово на имя" (proper-noun regex + стоп-слова + пороги) — на реальном транскрипте это заводило отдельную запись на каждое капитализированное слово прозы ("Looks", "Holds", "Not"); переделано полностью по прямой жалобе владельца. Английская проза; пол — не определяется морфологически, а задаётся пользователем при добавлении персонажа. **`hasPresenceVeto()`/`findDepartureSubject()`** (ROADMAP.md 5.41) — идея (не код) из PresenceEngine чужого MIT-экстеншена `ryzendigo/scene-director`: упоминание персонажа в контексте отсутствия ("thinks of", "misses") не делает его подлежащим; явный уход ("leaves", "exits") выметает его из локального стека подлежащих до конца сообщения. |
| **Сопоставление упоминаний локаций с картой** (`findMostRecentLocationMention`) | Ядра | ✅ [libraries/core/location-mention.js](libraries/core/location-mention.js) | Написана ДО [Ядра навигации по упоминаниям](cores/map-narration/index.js) (ROADMAP.md 5.59). Тот же closed-set-принцип, что уже у `speaker-detection.js`'s `findKnownMention()` (см. запись выше и [[feedback-speaker-detection-testing]]) — сопоставление только с ИЗВЕСТНЫМИ именами узлов карты, whole-word, регистронезависимо, никакого угадывания новой локации из прозы. Среди последних N сообщений и внутри одного сообщения побеждает САМОЕ ПОЗДНЕЕ совпадение ("куда только что перешёл разговор"), не самое раннее. Единственный текущий потребитель — Ядро навигации по упоминаниям. |
| **Математика глубины вставки в промпт** (`computeInsertIndex`) | Общая | ✅ [libraries/shared/chat-injection.js](libraries/shared/chat-injection.js) | Вынесена на 4-м реальном потребителе (ROADMAP.md 5.59) — была продублирована ИДЕНТИЧНЫМ кодом в `modules/notebook/index.js`/`modules/tools/notebook.js`/`modules/tools/secrets.js` (правило этого файла "выносить на втором потребителе" было давно просрочено). `depth` сообщений от конца `chat`, `depth=0` — "прямо перед тем, что сейчас отвечает модель" (`@0`, как у Alpha). Три Модуля теперь ре-экспортируют функцию из Библиотеки (`export {computeInsertIndex} from '...'`), тот же приём обратной совместимости, что уже у `drop-classify.js`/Picture Panel. |
| **Авто-палитра цвета** (`resolveBaseHue`/`computeAutoSpeakerColor`/`hexToRgba`) | Общая | ✅ [libraries/shared/color-palette.js](libraries/shared/color-palette.js) | Чистая HSL-математика — по кругу от акцентного цвета темы ST, детерминированно (N-й новый говорящий всегда получает тот же цвет при повторном резолве того же чата). `hexToRgba()` (ROADMAP.md 5.60) — hex+альфа в CSS `rgba(...)`, та же математика, что `color-mix()` уже делает в CSS, но в JS — нужна там, где инлайновому `style.fill` неоткуда взять переменную (карта, полупрозрачный регион с кастомным цветом). Единственные текущие потребители — [Модуль «Speaker Colors»](modules/speaker-colors/index.js) (авто-цвет) и [Модуль «Карта»](modules/map/index.js) (`hexToRgba`), но математика не завязана ни на один из них. |
| **Граф карты локаций** (`sanitizeNodeInput`/`sanitizeEdgeInput`/`wouldCreateCycle`/`resolveLocalMapHost`/`computeLocalScale`/`computeDistanceBetweenNodes`/`resolveEdgeDistance`/`computeTravelTimeMinutes`/`detectPolygonAdjacency`/`findAdjacentNodeIds`/`findNodesWithinDistance`/`isPointInPolygon`/`findContainingNodeId`/`snapPointToNearbyVertex`/`findRoutes`) | Ядра | ✅ [libraries/core/map-graph.js](libraries/core/map-graph.js) | Написана ДО [Ядра карты](cores/map/index.js), как требует принцип этого файла (ROADMAP.md 5.42). Контейнмент (дерево `parentId`, произвольная глубина) намеренно отделён от смежности (граф рёбер "можно дойти пешком") — жёсткая схема из 7 фиксированных уровней (Страна...Помещение), предложенная владельцем изначально, отброшена как форсированная эвристика (тот же класс ошибки, что уже исправлен в `speaker-detection.js`); `rank` — свободное число, 7 пунктов остались UI-подсказкой, не схемой. Координаты нормализованы (0..1) ЛОКАЛЬНО — относительно ближайшего предка, хостящего свою `localMap`, а не глобально — отсюда бесплатно следует "внутри здания можно ходить, между двумя разными зданиями по комнатам нельзя" (`computeDistanceBetweenNodes()` возвращает `null` при разных локальных пространствах, единственный мост — ручное ребро с `distanceOverrideUnits`). Масштаб вложенных уровней без явного `localMap` наследуется делением на `levelScaleCoefficient` (дефолт 15, владелец: "в 10/20 раз ниже"). `findRoutes()` — Dijkstra + top-k петлевых маршрутов через упрощённый алгоритм Йена, с `settings.minTraversableRank` — потолком абстракции, по прямой просьбе владельца ограничить "ускоренное перемещение" через слишком высокие уровни. `findNodesWithinDistance()` (ROADMAP.md 5.50, владелец: "построение соединений требует границ... Добавь авто-дистанцию по расстоянию между границами") — авто-соединение по реальной дистанции как альтернатива касанию границ полигона, работает и для простых маркеров; выключено по умолчанию (`settings.autoConnectDistanceUnits: null`). `isPointInPolygon()`/`findContainingNodeId()` (ROADMAP.md 5.53, владелец: "я всё ещё не могу добавить локацию внутрь региона физически нажав на него... Убери фишку с parental в меню, это должно быть автоматически") — ray-casting point-in-polygon (учитывает вогнутые полигоны) + выбор САМОГО МАЛЕНЬКОГО из вложенных совпадений (формула Гаусса на площадь) — контейнмент новой локации теперь решается геометрией клика, не выбирается вручную из списка. `snapPointToNearbyVertex()` (ROADMAP.md 5.60, владелец: "если регионы очень близко друг к другу... нужно их подтягивать для формирования границ") — притягивает новую точку полигона к ближайшей вершине соседнего региона в пределах ТОГО ЖЕ порога, что и `findAdjacentNodeIds()`'s собственное определение касания границ, гарантируя, что снапнутая точка реально засчитается как смежная. `sanitizeNodeInput()` также несёт `color`/`radius` (тот же ROADMAP.md 5.60) — per-node кастомизация внешнего вида, проходит через существующие контракты `map.nodes.create/update` без единого изменения в Ядре карты. Первый и пока единственный потребитель — Ядро карты. |
| **Классификация drop/paste** (`classifyDrop`/`isHttpUrl`) | Общая | ✅ [libraries/shared/drop-classify.js](libraries/shared/drop-classify.js) | Вынесена из [Ядра «Картинка»](cores/ui/picture-panel.js) при появлении второго реального потребителя — [Модуля «Карта»](modules/map/index.js)'s dropzone (ROADMAP.md 5.44), ровно тот сценарий, ради которого Библиотеки и выделяются постфактум (принцип в начале этого файла). Чистая: файл vs ссылка vs "ничего" по `dataTransfer`, без DOM и без сети. Картинка использует оба исхода (эфемерный показ), карта — только `kind: 'file'` (ссылку сознательно отклоняет, см. запись Ядра карты ниже). |
| **Апскейл картинки** (`sanitizeScaleFactor`/`computeClampedScaleFactor`/`computeUpscaledImage`/`cropImagePixels`) | Общая | ✅ [libraries/shared/image-upscale.js](libraries/shared/image-upscale.js) | Локальный (без сети/AI-сервиса — владелец выбрал это явно, ROADMAP.md 5.51) сепарабельный бикубический (Catmull-Rom) ресемплинг над плоскими `{data,width,height}` RGBA-буферами — та же форма, что у DOM'ного `ImageData`, но намеренно НЕ он сам, ради тестируемости в Node без canvas (decode/encode Blob↔пиксели — платформенная работа Модуля, не Библиотеки, тот же приём, что уже у `createImageBitmap`). `computeClampedScaleFactor()` — потолок `MAX_UPSCALE_DIMENSION = 8000` на любую сторону, сжимает запрошенный множитель, никогда не увеличивает. **Реальный баг перфоманса** пойман на большом тестовом изображении, не на маленьком: наивная версия пересчитывала веса ядра на КАЖДЫЙ пиксель через `.map()`-аллокацию — 16.6с на 5000×3000→8000px; переписано на предвычисленные плоские typed-array таблицы (веса/индексы считаются один раз НА ОСЬ, не на каждую строку/столбец) — 1.1с, тот же результат. `cropImagePixels()` (ROADMAP.md 5.52) — вырезает прямоугольник ИЗ УЖЕ декодированных пикселей ДО апскейла, добавлена, когда апскейл перешёл на тайловую модель (вырезать сектор картинки, а не апскейлить её всю целиком). Единственный текущий потребитель — [Модуль «Карта»](modules/map/index.js) (тайловый апскейл сектора холста по зуму), но алгоритм общий, не завязан на карту. |
| **Pan/zoom + квадродерево тайлов карты** (`FULL_VIEWPORT_RECT`/`viewportZoomScale`/`clampViewportRect`/`computeZoomedViewportRect`/`computePannedViewportRect`/`computeQuadtreeLevel`/`listVisibleTiles`/`tileCacheKey`/`computeTileSourcePixelRect`) | Общая | ✅ [libraries/shared/map-viewport.js](libraries/shared/map-viewport.js) | ROADMAP.md 5.52, владелец: "Почему по кнопке? Надо разбивать картинку на сектора, апскейлить только их при зуме." Чистая математика, ноль DOM/canvas/сигналов — тот же разрез слоёв, что у `map-graph.js`/Ядра карты. `viewportRect` — нормализованный (0..1) квадрат В ТОМ ЖЕ пространстве координат, что уже используют узлы/рёбра карты; `computeZoomedViewportRect()` зумит К КУРСОРУ (курсор остаётся на той же относительной позиции внутри нового прямоугольника); `listVisibleTiles()` возвращает ТОЛЬКО тайлы данного уровня, реально пересекающиеся с текущим вьюпортом — не всю `2^L x 2^L` сетку целиком, это и есть "квадродерево = динамическая детализация", а не фиксированная сетка. `computeTileSourcePixelRect()` (ROADMAP.md 5.56, реальный баг: соседние тайлы визуально РВАЛИ контент на стыке при сильном зуме) — округляет ГРАНИЦЫ тайла (не производную ширину/высоту), так что `Math.round()` одного и того же общего граничного значения у двух соседних тайлов детерминированно даёт идентичный целый пиксель; без этого `cropImagePixels()`'s собственное внутреннее округление расходилось с тем, что вызывающий код "думал" вырезал, на субпиксельную величину — умноженную на коэффициент апскейла, это становилось несколькими реальными пикселями смещения. `tileCacheKey()` (ROADMAP.md 5.57, реальный баг: тот же самый шов "исправили", но владелец продолжал его видеть) теперь берёт ОБЯЗАТЕЛЬНЫЙ параметр `algorithmVersion` — персистентный кэш секторов (`image.*`) не имеет ни истечения, ни GC, так что без версии в ключе сектор, посчитанный СТАРЫМ багованным кодом, отдавался бы вечно даже после исправления самого алгоритма; Модуль объявляет свою константу `TILE_ALGORITHM_VERSION` и обязан увеличивать её при каждом изменении того, что попадает в байты тайла. Единственный текущий потребитель — [Модуль «Карта»](modules/map/index.js), но математика не завязана на карту конкретно. |

## Предложены по паттернам из Alpha — не оценены явно

| Библиотека | Лагерь (предположительно) | Зачем нужна |
|---|---|---|
| **Реконсиляция динамической коллекции** ("сравни текущее с прошлым, добавь недостающее, убери лишнее") | Ядра | Этот паттерн в Alpha повторялся минимум 4 раза руками (DataBus reserve/unreserve, Macros registeredIds, Tracker pollTimers, LorebookService republishAll) — в Beta он уже встроен в саму Шину для контрактов/событий, но автору Ядра может понадобиться та же логика для СВОЕЙ внутренней коллекции, не связанной с Шиной напрямую. |
| **Безопасное выполнение** (обернуть вызов, получить `{ ok, value/error }`, никогда не бросить) | Общая | Шина уже даёт эту гарантию НА ГРАНИЦЕ (см. "Контракт на ошибки"), но и Ядру, и Модулю часто нужна та же дисциплина для СВОЕЙ внутренней логики, не проходящей через Шину. |
| **Вставка текста по курсору с сохранением каретки** | Модули | В Alpha дублировалось у Macros (`insertAtCursor`) и у токен-пикеров Tracker/RP Time — типовая вещь для "кликни — вставь в текстовое поле". |
